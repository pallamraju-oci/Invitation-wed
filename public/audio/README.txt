Place the background track here as:

  venkateswara-namam.mp3

The MusicToggle component (src/components/MusicToggle.tsx) references
/audio/venkateswara-namam.mp3. Music never autoplays; it only starts when
the visitor taps the button. Until a real file is added here, tapping the
button will simply fail to play (silently) rather than error the page.

Note: I can't generate or fetch the actual audio file myself — add a
licensed/authorized recording of Venkateswara Namam under this exact
filename.
